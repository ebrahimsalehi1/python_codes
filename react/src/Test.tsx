// This Component is for Test react,
// I wanna be more strond than before and just keep going with concenpts rather that just ask CoPilot to do that

interface TestProps {
  name?: string;
}

export const Test = ({
  name,
}: TestProps) => {
  return (
    <div>
      <div>{name}</div>
      HELLO TEST
    </div>
  );
};
